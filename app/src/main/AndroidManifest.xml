<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Camera hardware required — app won't install on devices without a camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- [ANDROID] Runtime permission — requested in MainActivity.onStart() -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- [DEEPAR] Required for video recording with audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- [ANDROID] Save screenshots/videos to external storage. Not needed on Android 13+ (API 33+) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" /> <!-- [DEEPAR] DeepAR validates the license key over the network on first launch -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
         [DEEPAR] Optional OpenCL acceleration for GPU-based face tracking.
             required="false" = app works without it, just falls back to CPU.
        -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL-pixel.so"
            android:required="false" />
        <!--
 [ANDROID] MainActivity is the only entry point.
             android:exported="true" is required on Android 12+ for activities with intent-filters.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>